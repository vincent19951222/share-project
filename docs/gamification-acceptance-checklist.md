# Gamification Acceptance Checklist

> 目标：用一份固定测试数据，把 GM-01 到 GM-19 的主链路验收到“能闭环、能解释、能运营观察”的状态。

## 测试环境

从仓库根目录执行。

```bash
# .env 应指向测试库
DATABASE_URL="file:/Users/vincent/data/share-project/dev.db"

# 重新生成验收数据
npx tsx scripts/fill-gamification-test-data.ts

# 启动本地服务
npm run dev
```

打开 `http://localhost:3001`。如果 3001 被占用，可以改用 `npm run dev -- --port 3002`。

## 已准备的测试数据

数据范围：`2026-04-01` 到 `2026-05-02`。

账号密码：

| 用户 | 角色 | 密码 | 用途 |
| --- | --- | --- | --- |
| `li` | ADMIN | `0000` | 管理员确认兑换、发布周报、测试高库存账号 |
| `luo` | MEMBER | `0000` | 主验收账号：任务、boost、打卡、社交、兑换 |
| `liu` | MEMBER | `0000` | 全队社交响应、强 boost 道具 |
| `wu` | MEMBER | `0000` | 请假券、社交邀请 |
| `ji` | MEMBER | `0000` | 任务换班、赛季冲刺券 |

基础统计：

| 用户 | 健身打卡 | 咖啡 | 银子 | 抽奖券 |
| --- | ---: | ---: | ---: | ---: |
| `li` | 26 | 35 | 890 | 26 |
| `luo` | 23 | 27 | 530 | 23 |
| `liu` | 19 | 19 | 430 | 19 |
| `wu` | 17 | 24 | 350 | 17 |
| `ji` | 17 | 23 | 380 | 17 |

预置背包：

| 用户 | 道具 |
| --- | --- |
| `li` | 瑞幸咖啡券 x2、小暴击券 x1、团队小喇叭 x1 |
| `luo` | 任务换班券 x1、小暴击券 x1、点名喝水令 x1、瑞幸咖啡券 x1 |
| `liu` | 双倍牛马券 x1、全员起立令 x1 |
| `wu` | 健身请假券 x1、出门溜达令 x1 |
| `ji` | 任务换班券 x1、赛季冲刺券 x1、红盘情报令 x1 |

## 1. 端到端冒烟测试

### 登录和补给站加载

- [ ] 用 `luo / 0000` 登录成功。
- [ ] 进入主界面后能看到打卡、共享看板、咖啡、日历、战报中心、补给站等入口。
- [ ] 进入“牛马补给站”，页面能加载今日四维任务、抽奖券余额、背包库存和弱社交雷达。
- [ ] 今日四维任务如果未生成，页面加载后会自动生成四张任务卡。

通过标准：

- 页面没有 500 或空白。
- 抽奖券余额显示为当前用户余额。
- 背包里能看到预置道具。

### 今日任务和生活券

建议仍使用 `luo`。

- [ ] 在补给站点击一个未完成任务的“换一个”，确认任务卡变化且换卡按钮变灰或次数变为不可再换。
- [ ] 点击四个任务的“我完成了”。
- [ ] 四项完成后点击“领取生活券”。
- [ ] 领取后“今日生活券”显示已到账，抽奖券余额增加 1。
- [ ] 再点领取按钮不会重复加券。

通过标准：

- 每个维度每天最多免费换一次。
- 四维全部完成前不能领生活券。
- 生活券只发一次。

### 健身打卡和 boost

建议使用 `luo`。验收数据里 `luo` 在 `2026-05-02` 还没有打卡。

- [ ] 在补给站背包选择“小暴击券”，点击“今日使用”。
- [ ] 今日效果区出现待生效 boost。
- [ ] 切到打卡面板，完成今日健身打卡。
- [ ] 回到补给站，今日效果变成已结算。
- [ ] 活动流出现打卡奖励，奖励金额包含 boost 结算。

通过标准：

- boost 使用后会预占库存。
- 真实打卡后 boost 从 `PENDING` 变为 `SETTLED`。
- 用户银子增加值等于 `PunchRecord.assetAwarded`。

### 抽奖和背包

建议使用 `li` 或 `luo`，两者都有足够抽奖券。

- [ ] 点击“单抽 x1”，确认抽奖券减少 1，并展示本次奖励。
- [ ] 点击“十连 x10”或“补券十连”，确认抽奖记录展示 10 个结果。
- [ ] 连续十连后如果只剩 3-6 张券，在银子足够时仍能用“补券十连”补齐到 10 张。
- [ ] 如果抽到道具，背包对应库存增加。
- [ ] 如果抽到银子，用户银子增加。
- [ ] 最近抽奖记录能在补给站抽奖区看到归档提示。
- [ ] 点击抽奖机区域的“查看抽奖概率”，能跳到 `/docs?tab=rules#supply-station-probability`。
- [ ] 抽奖概率说明显示 active 总权重 100、分层权重、直接银子期望和 active rewards。

通过标准：

- 单抽 / 十连不会让抽奖券余额变负。
- 十连补券价格为 40 银子 / 张，每人每天最多补 10 张。
- 十连不足 10 张券时，余额和当日补券额度足够就可以补齐，不再要求先攒到 7 张券。
- 奖励结算和抽奖记录在同一次操作后同时可见。

### 弱社交

建议先用 `luo` 发，再用 `li` 响应。

- [ ] 用 `luo` 在背包选择“点名喝水令”。
- [ ] 选择收件人 `li`，可选填写一句提醒，点击“今日使用”。
- [ ] `luo` 的“我发出的”里出现一条邀请。
- [ ] 登出后用 `li / 0000` 登录。
- [ ] 进入补给站，“我收到的”里看到邀请。
- [ ] 点击“响应”，邀请状态或响应数更新。

通过标准：

- 发邀请会扣减社交道具库存。
- 响应不会发银子、发券或推进赛季。
- 企业微信未配置时，本地邀请仍然成功。
- 点名喝水这类单点邀请只出现在发送方 / 接收方补给站，不写入团队动态；团队小喇叭、多人响应、稀有抽奖和周报才进入团队动态。

### 真实福利兑换

建议用 `luo` 申请，用 `li` 确认。

- [ ] 用 `luo` 在背包选择“瑞幸咖啡券”，点击“申请兑换”。
- [ ] `luo` 的“我的兑换”里出现待管理员确认记录，瑞幸券库存减少 1。
- [ ] 用 `li / 0000` 登录。
- [ ] 在补给站管理员“待处理兑换”里看到 `luo` 的申请。
- [ ] 点击“确认已兑换”。
- [ ] 记录状态变为已确认兑换。

可选取消路径：

- [ ] 重新生成验收数据。
- [ ] 重复申请兑换。
- [ ] 管理员点击“取消并返还”。
- [ ] 用户瑞幸券库存返还 1。

通过标准：

- 申请时扣库存。
- 确认后不返还库存。
- 取消后返还库存。
- 普通成员不能处理管理员队列。

### 战报和团队动态

建议使用 `li`。

- [ ] 进入“战报中心”。
- [ ] 找到牛马补给周报面板。
- [ ] 周报指标能展示四维完成率、本周发券、抽奖次数、弱社交响应。
- [ ] 点击“发布到团队动态”。
- [ ] 去团队动态页面或喇叭面板，确认出现本周游戏化周报动态。
- [ ] 再次发布同一周，不应生成重复动态。

通过标准：

- 周报读取不改变用户资产。
- 发布只创建或复用 TeamDynamic。
- 企业微信发送失败或未配置不影响本地发布。

## 2. 经济数据校验

每次完整验收后跑下面的只读 SQL。

```bash
sqlite3 /Users/vincent/data/share-project/dev.db '
select
  u.username,
  u.coins as userCoins,
  coalesce(sum(p.assetAwarded), 0) as punchCoins,
  u.ticketBalance as userTickets,
  coalesce((select sum(delta) from LotteryTicketLedger l where l.userId = u.id), 0) as ledgerTickets
from User u
left join PunchRecord p on p.userId = u.id and p.punched = 1
group by u.id
order by u.createdAt;
'
```

基础验收数据的通过标准：

- `userCoins = punchCoins`
- `userTickets = ledgerTickets`

如果验收过程中做了抽奖：

- `userCoins` 可能大于 `punchCoins`，差额应能从抽奖金币奖励减去补券银子消耗解释。
- `userTickets` 仍应等于 `LotteryTicketLedger.delta` 汇总。

追加校验：

```bash
sqlite3 /Users/vincent/data/share-project/dev.db '
select min(dayKey), max(dayKey), count(*) from PunchRecord;
select min(dayKey), max(dayKey), count(*) from CoffeeRecord;
select count(*) from LotteryDraw;
select count(*) from LotteryTicketLedger;
select status, count(*) from RealWorldRedemption group by status;
'
```

通过标准：

- 健身和咖啡数据覆盖 `2026-04-01` 到 `2026-05-02`。
- 抽奖后 `LotteryDraw` 与 `LotteryDrawResult` 有对应记录。
- 兑换记录只出现 `REQUESTED`、`CONFIRMED`、`CANCELLED`。

## 3. 管理员流程验收

管理员账号：`li / 0000`。

- [ ] 进入 `/admin` 后能先看到“运营观察”，再看到“配置总览”和“赛季设置”。
- [ ] “运营观察”显示当前 7 天窗口、抽奖券净变化、抽奖次数、待处理兑换、弱社交响应率、真实福利产出、企微失败。
- [ ] 风险卡能显示资产一致性、抽奖券囤积、兑换处理、弱社交频率、真实福利成本、企业微信发送六类观察结果。
- [ ] 待处理兑换队列能看到待管理员处理的真实福利申请，且只读展示不改变兑换状态。
- [ ] 高频点名关系、抽奖券余额排行、银子余额排行能正常展示。
- [ ] 点击“刷新观察”后面板重新加载，不创建抽奖、兑换、动态或企业微信记录。
- [ ] `/admin` 能看到“配置总览”。
- [ ] “配置总览”显示配置校验通过。
- [ ] 四维任务卡池能看到四个维度、启用任务数、启用权重和代表任务卡。
- [ ] 抽奖奖池能看到 active 总权重 100、分层权重、直接银子期望、active rewards 和 disabled rewards。
- [ ] 道具配置能看到分类、使用时机、效果摘要、使用限制和奖池可用性。
- [ ] 点击“刷新配置”后面板重新加载，不创建抽奖、兑换、动态或企业微信记录，也不修改任何用户资产。
- [ ] 管理员可以看到兑换待处理队列。
- [ ] 管理员可以确认兑换。
- [ ] 管理员可以取消兑换并返还库存。
- [ ] 管理员可以发布牛马补给周报到团队动态。
- [ ] 管理员重复发布同一周周报时复用已有动态。
- [ ] 普通成员看不到可处理的管理员兑换队列。
- [ ] 普通成员调用管理员确认 / 取消接口时应返回 `403`。

建议接口级复查：

```bash
npm test -- __tests__/gamification-config-observatory-api.test.ts __tests__/gamification-ops-dashboard-api.test.ts __tests__/gamification-redemption-api.test.ts __tests__/gamification-weekly-report-api.test.ts
```

## 4. 文档总表更新

- [ ] `docs/gamification-dev-roadmap.md` 当前状态表包含 GM-17。
- [ ] `docs/gamification-dev-roadmap.md` 当前状态表包含 GM-18。
- [ ] `docs/gamification-dev-roadmap.md` 当前状态表包含 GM-19。
- [ ] 文档中有 `## GM-16: Card Pool Tuning` 章节。
- [ ] 文档中有 `## GM-17: Ops Dashboard` 章节。
- [ ] 文档中有 `## GM-18: Config Observatory` 章节。
- [ ] 文档中有 `## GM-19: Probability Disclosure` 章节。
- [ ] 相关文档列表包含 GM-16 spec 和 plan。
- [ ] 相关文档列表包含 GM-17 spec 和 plan。
- [ ] 相关文档列表包含 GM-18 spec 和 plan。
- [ ] 相关文档列表包含 GM-19 spec 和 plan。
- [ ] 更新记录包含 `2026-05-02: GM-16 完成` 和 `2026-05-02: GM-17 完成`。
- [ ] 更新记录包含 `2026-05-02: GM-18 完成`。
- [ ] 更新记录包含 `2026-05-02: GM-19 完成`。
- [ ] 本验收 checklist 路径可被团队成员找到：`docs/gamification-acceptance-checklist.md`。

## 5. 奖池 / 道具 / 兑换运营风险清单

| 风险 | 验收信号 | 处理建议 |
| --- | --- | --- |
| 银子通胀 | 用户银子持续增长，但可消费场景只有十连补券 | 观察一周后决定是否增加银子消耗入口，或降低金币奖励权重 |
| 抽奖券囤积 | 用户每日发券大于抽奖消耗，`ticketBalance` 长期上升 | 调整任务券领取门槛、十连吸引力或券有效期策略 |
| 抽到不可用道具 | 背包出现“使用入口还没开放”的 active 奖励 | GM-16 校验应阻止；新增奖励前必须跑 content test |
| 真实福利履约延迟 | `REQUESTED` 兑换长期堆积 | 管理员需要固定处理节奏，必要时加待处理提醒 |
| 瑞幸券库存风险 | 用户抽到真实福利，但线下预算不足 | 上线前确认真实福利预算和每周发放上限 |
| 弱社交骚扰 | 同一用户频繁点名同一成员 | 观察 SocialInvitation 频率，必要时加同对象冷却 |
| 企业微信失败 | 本地邀请成功但群里没提醒 | 检查 `EnterpriseWechatSendLog`，未配置 webhook 时接受 `SKIPPED` |
| boost 滥用 | 单用户短期大量使用强 boost | 当前有每日 / 每周限制；验收时确认强 boost 一周最多一次 |
| 兑换取消争议 | 管理员取消后用户不清楚库存是否返还 | 检查 `InventoryItem` 数量和兑换状态文案 |
| 观察面板误当修复工具 | 管理员看到风险后以为系统已自动修复 | GM-17 只读；资产修复、阈值配置、奖池调整放到后续 GM |
| 观察窗口误读 | 近 7 天指标被当成全量历史 | 面板标题显示窗口范围；涉及资产一致性时同时使用全量可重算流水 |
| 配置总览误当编辑后台 | 管理员以为能在页面直接改奖池或任务卡 | GM-18 只读；配置变更仍通过代码评审和发布流程 |
| 禁用奖励被误解为废弃 | disabled rewards 仍在配置总览出现 | 面板明确区分 active / disabled，历史抽奖快照仍可解释 |
| 概率披露与实际奖池不一致 | 文档写死概率，奖池代码已变 | GM-19 概率明细从 reward pool helper 推导，不手抄奖励列表 |
| 用户误解权重为承诺出货 | 单次抽奖结果和百分比不一致 | 文档说明权重是长期近似概率，单抽不保证 |
| 测试库误操作生产库 | seed / acceptance 脚本写到了生产库 | 脚本已限制目标路径；执行前确认 `.env` 指向 `/Users/vincent/data/share-project/dev.db` |

## 上线前判定

必过：

- 端到端冒烟测试全部通过。
- 经济数据校验无无法解释的差额。
- 管理员兑换和周报发布流程通过。
- GM-16 文档和奖池校验测试通过。
- GM-17 运营观察面板、API 权限和只读刷新通过。
- GM-18 配置总览、API 权限和只读刷新通过。
- GM-19 抽奖概率说明、Docs Center 深链和补给站概率入口通过。

可带观察上线：

- 银子通胀。
- 抽奖券囤积。
- 真实福利履约节奏。
- 弱社交使用频率。
- GM-17 风险卡里的 watch 项。

不建议上线：

- 用户资产和流水无法对齐。
- 抽奖能产出当前不能使用、不能兑换、也没有明确展示状态的 active 奖励。
- 管理员无法处理真实福利兑换。
- 周报发布会改变用户资产或重复创建同周动态。
- 运营观察刷新会修改用户资产、兑换状态、团队动态或企业微信发送日志。
- 配置总览刷新会修改用户资产、抽奖记录、兑换状态、团队动态或企业微信发送日志。
